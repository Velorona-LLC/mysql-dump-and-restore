# Example environment variables for backup.sh and restore.sh
# Copy this file to .env and adjust values as needed.

# Source database configuration (used by backup.sh)
SOURCE_DB_USER=root
# SOURCE_DB_PASSWORD contains sensitive data — replace in .env
SOURCE_DB_PASSWORD=Password@123
SOURCE_DB_HOST=localhost
SOURCE_DB_PORT=3306
SOURCE_DB_NAME=db

# Dump type: "schema", "data", or "both"
DUMP_TYPE=both

# Table filtering options
# TABLE_MODE can be: "" (all tables), "selected", or "ignore"
TABLE_MODE=
# Tables separated by space for selected or ignored tables
SELECTED_TABLES=
IGNORED_TABLES=

# Target database configuration (used by restore.sh)
TARGET_DB_USER=root
# TARGET_DB_PASSWORD contains sensitive data — replace in .env
TARGET_DB_PASSWORD=Password@123
TARGET_DB_HOST=localhost
TARGET_DB_PORT=3306
TARGET_DB_NAME=db
# Defaults to <TARGET_DB_NAME>_backup
TARGET_DB_BACKUP_NAME=${TARGET_DB_NAME}_backup

# Dump file name produced/consumed by the scripts
DUMP_FILE=dump.sql

# Notes:
# - Copy this file to .env and keep secrets out of version control.
# - Values here mirror defaults from .env.sh; change as appropriate.
